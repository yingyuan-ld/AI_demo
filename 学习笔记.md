# LLM（Large Language Model，大语言模型）
- **含义**：以海量文本训练、擅长**理解与生成自然语言**的一类模型；对话、摘要、翻译、推理等都建立在其之上。

# Token
- **含义**：模型处理文本时的**最小计价/计长单位**（常是子词片段，不等于一个汉字或英文单词）；接口里的用量、计费、上下文上限多按 Token 计算。

# Context（上下文）
- **含义**：模型在**单次推理**时能同时「看到」的全部文字，通常包括系统说明、历史对话、检索片段、工具返回等；超出窗口的旧内容会被截断或遗忘，需靠摘要/RAG 等补救。

# Context Window（上下文窗口）
- **含义**：单次推理里，模型**最多能容纳**的上下文长度上限，通常用 **Token 数**表示。

# Prompt（提示词）
- **含义**：就是你平常发给大模型的那段输入文字。
- **引申**：User Prompt、 System Prompt。

# Prompt Enginerring（提示词工程）
- **含义**：通过设计、改写、组织提示词，让大模型更稳定、更准确地完成目标任务的实践与方法集合。

# RAG（Retrieval-Augmented Generation，检索增强生成）
- **含义**：先把和问题相关的文档片段检索出来，再让大模型基于这些片段生成回答

# LLM wiki
- **含义**：由大模型持续**编译并维护**的一套结构化、互相链接的笔记（多为 Markdown Wiki），夹在人和原始资料之间；新资料进来时先被理解、提炼、交叉引用并写进 Wiki，查询时读的是这份已编译知识，而不是每次临时从原文检索（区别于 RAG）。

# Tool（工具）
- **含义**：指模型在对话之外，还能调用的外部能力：由程序实现，模型通常只负责调用。

# MCP（Model Context Protocol，模型上下文协议）
- **含义**：让 AI 应用和「外部能力」之间用同一套标准通信（包含服务器、工具、客户端）

# Agent（智能体）
- **含义**：以大模型为「大脑」、能按目标自主规划并反复行动的系统：常含工具调用，根据结果再规划下一步。

# Agent Skill（智能体技能）
- **含义**：写给 Agent 看的说明文档、（常见为 `SKILL.md` 等文档）：约定某类任务怎么做、用哪些步骤/术语/规范；在任务匹配时被读取并遵循。

# Fine tuning（微调）
- **含义**：在已预训练好的大模型上，用较小规模的专用数据继续训练、**更新模型权重**，使其更贴合特定任务、领域或输出风格；相对改提示词（Prompt）或外挂检索（RAG），改的是模型本身。

# KV Cache（Key-Value Cache，键值缓存）
- **含义**：自回归解码时，注意力需要「当前 Q × 历史各位置的 K、V」；把已生成 token 的 **K、V 缓存起来**，新 token 只算自己的并 **追加**，避免每步重算整段历史。
- **作用 / 代价**：显著 **加快推理**；代价是 **显存** 随上下文变长而增加，因此衍生 MQA、GQA、MLA 等 **减小 KV 占用与带宽** 的结构。

# MQA / GQA / MLA（KV 侧的注意力变体）
- **MQA（Multi-Query Attention，多查询注意力）**：每头仍有各自 **Q**，但 **整层共享一套 K、V** → KV cache 与读显存最省，表达力相对最「紧」。
- **GQA（Grouped-Query Attention，分组查询注意力）**：头分成若干 **组**，**组内** 共享 K、V，组间不同 → 在标准多头与 MQA 之间的折中（不少开源模型采用）。
- **MLA（Multi-head Latent Attention，多头潜空间注意力）**：K、V 经 **低秩 / 潜空间** 等压缩后再参与注意力（如 DeepSeek-V2 路线）→ 在尽量少伤效果的前提下 **再压 KV 体积**。

# 多模态（Multimodal）
- **含义**：不只处理单一类型信息，而是同时涉及多种 **模态**（信息载体），常见如 **文本、图像、音频、视频** 等。
- **多模态大模型**：能 **读多种输入**（例如图+文）、做 **跨模态** 理解与生成（看图问答、听语音再输出文字等）；常把不同模态 **映射/对齐到统一表示空间** 再交给语言模型等模块推理。


开发工具